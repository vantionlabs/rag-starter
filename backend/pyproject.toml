[project]
name = "rag-starter"
version = "0.1.0"
description = "Vantion RAG starter: event-driven ingestion, hybrid pgvector search and citations that are verified before an answer is shown"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.2",
    "pgvector>=0.3",
    "alembic>=1.14",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "pydantic-ai>=0.4",
    "openai>=1.60",
    "structlog>=24.4",
    "boto3>=1.35",
    "fastapi-users[sqlalchemy]>=14.0",
    "httpx>=0.28",
    "celery[redis]>=5.4",
    "redis>=5.0",
    "tiktoken>=0.8",
    # Tracing to Langfuse over OTLP (app/observability/tracing.py); a no-op
    # until the LANGFUSE_* keys are set.
    "opentelemetry-sdk>=1.29",
    "opentelemetry-exporter-otlp-proto-http>=1.29",
]

[project.optional-dependencies]
# PDF parsing is heavy (docling pulls torch); install only where ingestion
# of PDFs is needed: `uv sync --extra docling`. The API image stays slim.
docling = ["docling>=2.15"]
# Error tracking; the app no-ops when SENTRY_DSN is absent.
sentry = ["sentry-sdk[fastapi]>=2.20"]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "ruff>=0.8",
    "pyright>=1.1.390",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.flake8-bugbear]
# FastAPI's dependency-injection idiom: Depends(...) in argument defaults.
extend-immutable-calls = ["fastapi.Depends", "fastapi.security.HTTPBearer"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: needs live Postgres/Redis/OpenAI (deselect with '-m \"not integration\"')",
]

[tool.pyright]
include = ["app", "tests", "evals", "scripts"]
pythonVersion = "3.12"
typeCheckingMode = "basic"
